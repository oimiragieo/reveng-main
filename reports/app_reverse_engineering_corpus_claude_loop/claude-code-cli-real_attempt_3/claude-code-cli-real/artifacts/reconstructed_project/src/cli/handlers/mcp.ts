/**
 * Inferred pseudo module for `cli/handlers/mcp.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["mcpAddFromDesktopHandler", "mcpAddJsonHandler", "mcpGetHandler", "mcpListHandler", "mcpRemoveHandler", "mcpResetChoicesHandler", "mcpServeHandler"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: mcpAddFromDesktopHandler, mcpAddJsonHandler, mcpGetHandler, mcpListHandler, mcpRemoveHandler, mcpResetChoicesHandler, mcpServeHandler";
}
