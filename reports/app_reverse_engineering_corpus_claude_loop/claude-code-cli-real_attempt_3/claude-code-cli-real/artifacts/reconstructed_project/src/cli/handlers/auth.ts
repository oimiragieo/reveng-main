/**
 * Inferred pseudo module for `cli/handlers/auth.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["authLogin", "authStatus"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: authLogin, authStatus";
}
