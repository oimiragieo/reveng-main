/**
 * Inferred pseudo module for `cli/handlers/plugins.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["marketplaceAddHandler", "pluginInstallHandler"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: marketplaceAddHandler, pluginInstallHandler";
}
