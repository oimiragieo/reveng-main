/**
 * Inferred pseudo module for `tasks/cancel.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["tasks/cancel"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: tasks/cancel";
}
