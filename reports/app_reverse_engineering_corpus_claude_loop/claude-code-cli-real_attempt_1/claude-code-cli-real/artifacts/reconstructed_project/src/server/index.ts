/**
 * Inferred pseudo module for `server/index.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["server/index.js"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: server/index.js";
}
