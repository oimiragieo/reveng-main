/**
 * Inferred pseudo module for `cli/handlers/util.ts`.
 * Derived from recoverable bundle symbol names; not canonical upstream source.
 */

export const inferredSymbols = ["doctorHandler", "setupTokenHandler"] as const;

export function describeInferredModule(): string {
  return "Inferred from bundle symbols: doctorHandler, setupTokenHandler";
}
