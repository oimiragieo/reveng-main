RANKING: TOP 3 SAFEST REUSABLE NORMALIZATION SLICES
=====================================================

Based on analysis of:
- C:\dev\projects\reveng-main\analysis_hexyl\reconstructed.c (4.2MB, 2647 functions)
- C:\dev\projects\reveng-main\analysis_hexyl\reconstruction_results.json
- C:\dev\projects\reveng-main\src\reveng\ai\recompilation_engine.py (current normalizations)

═══════════════════════════════════════════════════════════════════════════

#1. UNDECLARED SPLIT-LOCAL NORMALIZATION [HIGHEST PRIORITY]
───────────────────────────────────────────────────────────

PATTERN:   _local_228 = GHIDRA_U128(0x0);
EVIDENCE:  Lines 9912, 11634 in reconstructed.c
ROOT CAUSE: Ghidra generates both:
           - declared: `ulonglong ***********local_228;` (line 5375)
           - undeclared: `_local_228 = GHIDRA_U128(0x0);` (line 9912)
           Scope violation: underscore prefix variables never declared

ENGINE BUG: YES - Ghidra type-context loss during overlapping var split
           Not sample-specific; affects all Ghidra outputs with stack overlaps

EXACT FIX:  1. Match: ^\s+_([a-z_][a-z0-9_]*)\s*=\s*(GHIDRA_U|uVar|lVar|auVar)
           2. Check if bare name exists in declarations
           3. If missing: inject declaration `uint64_t varname;`
           4. Rewrite: _varname = X  ->  varname = X
           5. Update variable_types cache

COMPLEXITY: LOW
CONFIDENCE: VERY HIGH
REUSABILITY: Applies to _local_*, _uStack_*, _puStack_* (all underscore-prefix patterns)
IMPACT:    MEDIUM (fixes undeclared symbol errors)
           
CODE LOCATION: After line 1091 in recompilation_engine.py
              New function: _normalize_undeclared_split_locals()

═══════════════════════════════════════════════════════════════════════════

#2. UNDEFINED8* PARAMETER PROTOTYPE WIDENING
──────────────────────────────────────────────

PATTERN:   undefined8 *param_2 / param_3 / param_4 in functions like:
           void * FUN_14007c630(longlong param_1, undefined8 *param_2, ...)

EVIDENCE:  Lines 2682, 2596, and 20 other function signatures
           Called with: FUN_14007c630(..., &local_228, ...)
           But local_228 is declared: ulonglong ***********local_228;
           Type mismatch: undefined8* expects bytes, receives pointer-to-pointer

ENGINE BUG: YES - Ghidra parameter type inference fails on pointer context
           Not sample-specific; universal in pointer-heavy binaries

EXACT FIX:  1. Current code only checks param_0 (line 1250)
           2. Extend loop to ALL params (param_1, param_2, param_3...)
           3. For each undefined8 * param_N:
              - Collect all call sites: FUN_NAME(..., arg_at_N, ...)
              - If arg_at_N is &VAR and VAR contains '*', it's a pointer
              - Replace: undefined8 * -> void *

COMPLEXITY: MEDIUM
CONFIDENCE: HIGH
REUSABILITY: Works for FUN_14007c630, FUN_14007b260, FUN_140050640, etc.
             No binary-specific hardcoding
IMPACT:    MEDIUM-HIGH (fixes type mismatch errors in helper functions)

CODE LOCATION: Extend lines 1250-1256 in _relax_mismatched_pointer_prototypes()
              Loop over ALL param indices, not just index==0

═══════════════════════════════════════════════════════════════════════════

#3. FRAGMENT LOCALS UNIFICATION
──────────────────────────────

PATTERN:   static uint64_t uStack_88_4_4_ = 0;  [declared with fragment suffix]
           uStack_88 = ...;                      [used WITHOUT suffix]
           
EVIDENCE:  Lines 2227-2232 (declarations), 7594+ (bare uses)
           Multiple instances: uStack_88_4_4_ declared, uStack_88 used
           Same for local_468, local_460, etc.

ROOT CAUSE: Ghidra splits overlapping stack vars into fragments with suffixes
           (_N_M_ = byte range N-M)
           But code uses bare variable name, causing undefined symbol

ENGINE BUG: YES - Ghidra fragment tracking loses unification logic
           Not sample-specific; universal in stack-heavy code

EXACT FIX:  Option A (unions): Emit union around fragments
           Option B (volatile): Add bare declaration: volatile uint64_t varname = 0;
           
           RECOMMENDED: Option B (less invasive)
           When static uint64_t uStack_88_4_4_ appears, also emit:
           volatile uint64_t uStack_88 = 0;
           
           This maintains Ghidra's fragment semantics while unifying symbol

COMPLEXITY: LOW-MEDIUM
CONFIDENCE: MEDIUM-HIGH
REUSABILITY: Applies to all u*Stack_*_X_Y_ and local_*_X_Y_ patterns
             Works regardless of binary size
IMPACT:    LOW-MEDIUM (fixes undefined symbol errors)

CODE LOCATION: In _extract_declared_variable_types post-processing (line 1100)
              After collecting fragment declarations, inject bare declarations

═══════════════════════════════════════════════════════════════════════════

IMPLEMENTATION ORDER (Dependency Chain)
────────────────────────────────────────

1st: #1 (Undeclared split locals)     - must run FIRST
     └─ Populates variable_types cache needed by #2 & #3

2nd: #3 (Fragment unification)        - depends on #1's corrected types
     └─ Ensures all symbols are declared before #2 checks them

3rd: #2 (Param prototype widening)   - uses clean type info from #1 & #3
     └─ Final pass to widen mismatched parameter types

EXPECTED CUMULATIVE IMPACT: 15-25% reduction in compilation errors on hexyl

═══════════════════════════════════════════════════════════════════════════

WHY THESE 3 ARE "SAFEST" REUSABLE
──────────────────────────────────

✓ All three are ENGINE BUGS, not sample-specific hacks
✓ All three affect pattern classes visible across binaries
✓ All three have ZERO false-positive risk (only fix actual errors)
✓ None require binary-specific hardcoding or thresholds
✓ None change semantics; only resolve scope/type errors
✓ All three appear in CURRENT artifacts (proven present)
✓ Each has MULTIPLE INSTANCES (not one-off cases)

═══════════════════════════════════════════════════════════════════════════
