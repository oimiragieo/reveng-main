# Wave 6 — R-RALPH-2 engine wedge from fingerprint evidence

> Thinktank gate required before large engine edits. L33 wave-scope.

**Goal:** Turn Wave 5 fingerprint attribution (operator-local: **232** first-party `src/` confirms on Bun extract; hermetic dual controls green) into the **smallest tracked-corpus engine wedge** that moves Ralph `best_project_file_recall` on the ship-gating micro-bundle toward 0.8 — without claiming exe decode or closing Phase 6.

**Evidence already banked (do not re-litigate):**
- Wave 4: tracked recall **0.4** via `source_map` materialize; mismatch 0.0
- Wave 5: hermetic fingerprint Tier A; dogfood map rebuild recall **1.0**; stale→Bun confirms **232**
- npm product path is `claude.exe` (no `cli.js`) — product RALPH-2 still packaging-blocked

## Candidate wedge (pick one after Thinktank)

| ID | Wedge | Ship gate |
|----|-------|-----------|
| W6-A | Wire fingerprint confirmations into JS adapter scorecard / recovered-path hints on tracked bundle | tracked recall lift vs Wave 4 baseline; mismatch still 0 |
| W6-B | Bundler-aware module graph scoring (P4-BUNDLER slice) using fingerprint paths as soft priors | same |
| W6-C | Stop/go research only if A/B cannot move tracked recall | Sol NO-GO documented |

## Non-goals

- Closing all backlog / enterprise GA / Phase 6
- Committing Anthropic trees
- VLQ stale-map remapping onto Bun bytecode
- Flipping M1-NATIVE-FAM `required:true` while `native_fallback_empty`

## M1-NATIVE-FAM (parallel honesty)

Probe v1.3: hello_go/hexyl process `completed` with `semantic_reason=native_fallback_empty`. **Do not flip `required:true`.** Wave 6 native sub-task: measure C/Go families; land research row; only flip after analyze produces non-empty native analysis_data ≤120s without Ghidra.
