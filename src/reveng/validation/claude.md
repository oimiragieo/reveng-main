# `claude.md` — `validation`

**Repository path:** `src/reveng/validation/`

Breadcrumb for AI navigation: this folder’s files, top-level Python symbols, and one-line intent.

## Python modules

### `__init__.py`
- **Summary:** REVENG v5.0 - Binary Validation Module

### `differential_fuzzer.py`
- **Summary:** Differential Fuzzing & Validation Engine
- **Classes:**
  - `DivergenceType` — Types of divergences between binaries
  - `ExecutionResult` — Result from executing a binary with specific input
  - `Divergence` — A divergence found between original and recompiled binary
  - `ValidationResult` — Result from differential fuzzing validation
  - `DifferentialFuzzingEngine` — Differential fuzzing engine for validating binary recompilation
  - `CoverageTracker` — Track code coverage during differential fuzzing
  - `DivergenceAnalyzer` — Advanced divergence analysis and root cause identification

---
*Generated or maintained for Claude / AI agents. Primary package: `src/reveng`.*
