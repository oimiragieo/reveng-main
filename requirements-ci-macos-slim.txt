# CI macOS slim requirements — excludes angr/unicorn/claripy.
# Rationale: unicorn source builds fail on modern CMake (cmake_minimum_required < 3.5).
# Upstream: https://github.com/unicorn-engine/unicorn/issues/2263 (accessed 2026-08-09)
# Wave 2 W2-3 / CI-UNICORN-BUILD-1 mitigation. Not a claim that angr is green.
# Generated from requirements.txt by filtering package lines matching ^(angr|unicorn|claripy).
# Keep this file committed and in sync when editing requirements.txt.

# REVENG Universal Reverse Engineering Platform - Core Dependencies
# =================================================================
# This file contains the core Python dependencies required for REVENG.
# For development dependencies, see requirements-dev.txt
# For Java analysis dependencies, see requirements-java.txt

# =============================================================================
# CORE DEPENDENCIES - REQUIRED for basic functionality
# =============================================================================

# HTTP client for API calls (security updates)
requests>=2.31.0,<3.0.0

# Web server for Ghidra HTTP API
flask>=2.3.0,<4.0.0
flask-cors>=4.0.0,<7.0.0

# Ghidra MCP integration for professional disassembly
# Note: ghidramcp is not available on PyPI, using fallback mode

# =============================================================================
# BINARY MANIPULATION - REQUIRED for reassembly/patching
# =============================================================================

# Binary parsing and patching (NOT optional!) - security updates
lief>=0.14.1,<0.18.0

# Multi-architecture assembler (NOT optional!)
keystone-engine>=0.9.2,<1.0.0

# Multi-architecture disassembler (NOT optional!)
capstone>=5.0.0,<6.0.0

# =============================================================================
# VISUALIZATION AND ANALYSIS - Recommended for full functionality
# =============================================================================

# Control flow graph analysis (security updates)
networkx>=3.4.2,<4.0.0

# Graph visualization (requires Graphviz)
pydot>=1.4.2,<2.0.0

# Progress bars for long-running operations (security updates)
tqdm>=4.66.0,<5.0.0

# Configuration file support (security updates)
pyyaml>=6.0.1,<7.0.0

# Secure model serialization (replaces unsafe pickle)
joblib>=1.3.0,<2.0.0

# =============================================================================
# MACHINE LEARNING & SCIENTIFIC COMPUTING - Required for ML features
# =============================================================================

# Core scientific computing (required by networkx and ML modules)
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0

# Machine learning (for vulnerability detection, malware classification, anomaly detection)
scikit-learn>=1.3.0,<2.0.0

# Data analysis (recommended for enhanced features)
pandas>=2.0.0,<3.0.0

# =============================================================================
# AI/ML SUPPORT - Optional but recommended
# =============================================================================

# Note: AI providers require separate setup:
# - Ollama: Install from https://ollama.ai (local LLM)
# - Anthropic: Set ANTHROPIC_API_KEY environment variable
# - OpenAI: Set OPENAI_API_KEY environment variable
# - Google: Set GEMINI_API_KEY environment variable

# Local LLM client used for Ollama-backed workflows
ollama>=0.6.1

# v4.0 NEW: Deep learning for advanced features (RECOMMENDED)
# PyTorch for GPU acceleration and LLM4Decompile
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0

# v4.0 NEW: LLM4Decompile specialized models
# Note: Models will be downloaded on first use (~18GB for 9B model)

# =============================================================================
# v4.0 NEW FEATURES - Advanced reverse engineering capabilities
# =============================================================================

# Symbolic Execution and Constraint Solving
z3-solver>=4.12.0,<5.0.0  # SMT solver for constraints

# Type Reconstruction and Analysis
pycparser>=2.21,<3.0.0  # C code parsing for type analysis

# Binary Lifting and LLVM Integration
# Note: Requires LLVM toolchain installed (see system requirements)
pyelftools>=0.29,<1.0.0  # ELF binary parsing

# Malware signature scanning
yara-python>=4.5.4
volatility3>=2.27.0

# Semantic Binary Diffing
scipy>=1.10.0,<2.0.0  # Already listed above, needed for Hungarian algorithm

# =============================================================================
# v5.0 NEW FEATURES - Next-generation reverse engineering capabilities
# =============================================================================

# Differential Fuzzing and Validation
# Note: AFL++ and libFuzzer require system installation
# Linux: sudo apt install afl++ clang-tools
# macOS: brew install afl-fuzz

# Exploit Chain Generation
# networkx>=3.4.2,<4.0.0  # Already listed above for graph-based analysis
# Note: ROPgadget tool recommended - pip install ropgadget

# Intel PT Hardware Analysis
# Note: Requires Intel CPU with PT support and Linux kernel 4.1+
# Install perf tools: sudo apt install linux-tools-generic
# Install libipt: sudo apt install libipt-dev

# LLM-Powered Deobfuscation
openai>=1.0.0  # OpenAI GPT-4 API client
anthropic>=0.7.0  # Anthropic Claude API client

# Agent SDK Dependencies
aiohttp>=3.9.0  # Async HTTP client/server for MCP transports

# Deep Learning Binary Classification
# torch>=2.0.0  # Already listed above
# transformers>=4.30.0  # Already listed above

# Cloud Platform (Kubernetes-based)
kubernetes>=28.0.0  # Kubernetes Python client
boto3>=1.28.0  # AWS SDK for cloud storage

# JIT Adaptive Compilation
# Uses existing LLVM toolchain

# =============================================================================
# SYSTEM REQUIREMENTS (NOT Python packages)
# =============================================================================

# 1. Java Development Kit (JDK) 21 (64-bit)
#    - Download from Oracle or OpenJDK
#    - Ensure 'java' is in your system's PATH
#    - Required for Ghidra integration

# 2. Ghidra (Professional disassembly engine)
#    - Download from https://github.com/NationalSecurityAgency/ghidra
#    - Extract to C:\ghidra (Windows) or /opt/ghidra (Linux)
#    - Set GHIDRA_INSTALL_DIR environment variable

# 3. Compiler toolchain (for binary reassembly)
#    - Linux: sudo apt install gcc clang clang-format cppcheck
#    - macOS: xcode-select --install && brew install clang-format cppcheck
#    - Windows: Install MinGW-w64 or Visual Studio Build Tools

# 4. v4.0 NEW: LLVM Toolchain (for binary lifting and optimization)
#    - Linux: sudo apt install llvm llvm-dev clang
#    - macOS: brew install llvm
#    - Windows: Download from https://releases.llvm.org/
#    - Verify: llvm-dis --version, opt --version, llc --version

# 5. v4.0 NEW: Build Acceleration Tools (optional but recommended)
#    - ccache: Compiler cache for 5-10x faster rebuilds
#      Linux: sudo apt install ccache
#      macOS: brew install ccache
#    - distcc: Distributed compilation (optional)
#      Linux: sudo apt install distcc
#      macOS: brew install distcc

# 6. v4.0 NEW: Binary Analysis Tools (optional)
#    - objdump: For disassembly (usually pre-installed)
#    - readelf: For ELF analysis (usually pre-installed)

# 7. Code quality tools (system packages)
#    - clang-format: Code formatting
#    - cppcheck: Static analysis
#    - black: Python code formatting (pip install black)
#    - pylint: Python linting (pip install pylint)
#    - mypy: Python type checking (pip install mypy)

# 8. v5.0 NEW: Fuzzing and Exploitation Tools
#    - AFL++: Coverage-guided fuzzing
#      Linux: sudo apt install afl++
#      macOS: brew install afl-fuzz
#    - ROPgadget: ROP chain generation
#      pip install ropgadget
#    - pwntools: Exploit development (optional)
#      pip install pwntools

# 9. v5.0 NEW: Hardware Analysis Tools
#    - perf: Linux performance analysis with Intel PT
#      Linux: sudo apt install linux-tools-$(uname -r)
#    - Intel Processor Trace support requires:
#      - Intel CPU with PT support (most modern Intel CPUs)
#      - Linux kernel 4.1+ with PT enabled

# =============================================================================
# INSTALLATION INSTRUCTIONS
# =============================================================================

# 1. Install Python dependencies:
#    pip install -r requirements.txt

# 2. Install development dependencies (optional):
#    pip install -r requirements-dev.txt

# 3. Install Java analysis dependencies (optional):
#    pip install -r requirements-java.txt

# 4. Verify installation:
#    python tools/check_toolchain.py --fix

# 5. Run basic test:
#    python reveng_analyzer.py --help
