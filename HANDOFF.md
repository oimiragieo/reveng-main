# Resume Note

Start here if the session context is lost:

- Read `docs/architecture/session-handoff-2026-03-29.md`
- Claude is the completed JS benchmark
- Droid (`C:\dev\droid.exe`) is the next native benchmark
- The next implementation step is native/Ghidra IR export using the shared IR in `src/reveng/ir.py`
